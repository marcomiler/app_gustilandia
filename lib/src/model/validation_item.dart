class ValidationItem {
  final String value;
  final String error;

  ValidationItem(this.value, this.error);
}

class ValidationDistrito {
  final String value;
  final String error;

  ValidationDistrito(this.value, this.error);
}
